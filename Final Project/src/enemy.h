#ifndef ENEMY_H
#define ENEMY_H

#include "SDL2/SDL.h"
#include "character.h"

class Enemy : public Character{
  public:

		// Enemy(int init_x, int init_y);
		Enemy();
		// Direction Heuristic_search();


};

#endif